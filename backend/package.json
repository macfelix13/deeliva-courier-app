{
  "name": "deeliva-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Placeholder REST backend for the Deeliva courier app. In-memory + JSON-file store standing in for the real Deeliva API — see README.md for how to swap it out.",
  "type": "commonjs",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.9",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  }
}
